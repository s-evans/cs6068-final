#include "timer.h"
#include "utils.h"

int main( int argc, char** argv )
{
    // TODO: parse input command line arguments
    // c / d - compress / decompress
    // s / p - serial / parallel
    // h - huffman
    // t - timing

    GpuTimer timer;
    timer.Start();

    timer.Stop();
}

